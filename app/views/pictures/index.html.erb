<h1>投稿一覧</h1>
<table>
  <tr>
    <th>内容</th>
    <th>画像</th>
  </tr>
  <% @pictures.each do |picture| %>
    <tr>
      <td><%= picture.content %></td>
      <td><%= picture.image %></td>
    </tr>
  <% end %>
</table>